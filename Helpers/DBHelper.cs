using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using System.Threading.Tasks;
using System.Windows;

namespace WalletWPF.Helpers
{
    public static class DBHelper
    {
        //private static MySqlConnection mySqlConnection; // connection on film
        //private static MySqlCommand mySqlCommand = null; // cmd on film
        //private static DataTable data; // dt o film
        //private static MySqlDataAdapter mySqlDataAdapter; // sda on film

        //public static void EstablishConnection()
        //{
        //    try
        //    {
        //        MySqlConnectionStringBuilder mySqlConnectionStringBuilder = new MySqlConnectionStringBuilder(); // builder on film
        //        mySqlConnectionStringBuilder.Server = "127.0.0.1";
        //        mySqlConnectionStringBuilder.UserID = "wallet";
        //        mySqlConnectionStringBuilder.Password = "wallet";
        //        mySqlConnectionStringBuilder.Database = "walletdb";
        //        mySqlConnectionStringBuilder.SslMode = MySqlSslMode.None;
        //        mySqlConnection = new MySqlConnection(mySqlConnectionStringBuilder.ToString());
        //        MessageBox.Show("Nawiązano połączenie z bazą danych", "Connection", MessageBoxButton.OK, MessageBoxImage.Information);
        //    }
        //    catch (Exception ex)
        //    {
        //        MessageBox.Show(ex.ToString());
        //    }
        //}
    }
}
